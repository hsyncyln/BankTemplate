var search = {

    loadUploadPage: function( item, url ) {
        // ajaxMethods.load(item,url);
        $(item).load(url);

    }
    // loadUploadPage: function() {
    //     let id = document.getElementById("IdentityNumber").value;
    //     let url = "/credit/update/" + id;
    //     search.loadUploadPage("#loadUpdate",url);
    // }
};